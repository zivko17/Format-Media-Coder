<UserControl x:Class="FormatMediaCoder.App.Views.AnalizadorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgApp}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="32,24">
        <StackPanel MaxWidth="720" HorizontalAlignment="Left">
            <TextBlock Text="Analizador" Foreground="{StaticResource Text1}" FontSize="22" FontWeight="Bold" />
            <TextBlock Text="Suelta un archivo y ve exactamente qué es: codec, resolución, fps, canal alfa, audio."
                       Foreground="{StaticResource Text2}" FontSize="13" Margin="0,4,0,18" TextWrapping="Wrap" />

            <Border Background="{StaticResource BgCard}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8" Padding="18">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding InputFile}" IsReadOnly="True" VerticalContentAlignment="Center" />
                        <Button Grid.Column="1" Content="📁  Elegir archivo" Style="{StaticResource GhostButton}" Margin="8,0,0,0" Command="{Binding PickCommand}" />
                    </Grid>

                    <TextBlock Text="{Binding Title}" Foreground="{StaticResource Text1}" FontSize="14" FontWeight="SemiBold" Margin="0,16,0,4" />
                    <TextBlock Text="{Binding StatusLine}" Foreground="{StaticResource Text2}" FontSize="12" />

                    <ItemsControl ItemsSource="{Binding Rows}" Margin="0,10,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="200" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="{StaticResource BgInput}" Padding="10,7">
                                        <TextBlock Text="{Binding Label}" Foreground="{StaticResource Text2}" FontSize="12" />
                                    </Border>
                                    <Border Grid.Column="1" Background="{StaticResource BgInput}" Padding="10,7" Margin="1,0,0,0">
                                        <TextBlock Text="{Binding Value}" Foreground="{StaticResource Text1}" FontSize="12" FontFamily="Consolas" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
